# Installed city resource evidence — September 26, 2026

Twelve selected city sessions passed the functional resource-admission campaign.
Each session completed three body ticks, explicit releases, Finish, and confirmed owner cleanup.
All 48 sampled process births retired.
This observation does not prove complete descendant containment.

The [machine-readable summary](native-city-resource-2026-09-26.json) binds the private receipts and tested scope.
Original commands, captures, payloads, and process observations remain private local evidence.
The public source and summary describe those observations without loaded-code attestation.

## Selected construction

The client uses published commit [`eea2a64e`](https://github.com/sepahead/CREBAIN/tree/eea2a64efbae3c34f74d4784d1ee0dc64cfba5e5).
The unchanged native runtime uses [`3fe34a1c`](https://github.com/sepahead/CREBAIN/tree/3fe34a1c9dd5b4cd575f6f1c3950e8ba8e51521c).
Their separate archive, wheel, installation, and runtime identities remain explicit.
Both fresh environments use the selected NCP SDK.
Only the captured environment installs Prisoma's transcript package.

Each group runs the legacy body, budgeted body, and budgeted captured routes.
All routes retain the original 16 solids, initial entity rosters, initial actions, and subsequent holds.
Every session has fresh run, endpoint, and generation identities.
No graphics or thermal source is selected.

| Entities | Sources | Sessions | Original payloads | Original bytes |
| ---: | --- | ---: | ---: | ---: |
| 1 | CPU only | 3 | 0 | 0 |
| 1 | Two pressure sources | 3 | 12 | 12,792 |
| 256 | CPU only | 3 | 0 | 0 |
| 256 | Two pressure sources | 3 | 12 | 12,792 |
| Total | | 12 | 24 | 25,584 |

Pressure publication periods are one and two model ticks.
The first microphone supplies 133, 133, and 134 samples.
The second supplies 133 samples at tick two.
The other second-microphone slots remain explicitly not due.
Both pressure groups match complete original bytes and typed meanings across all three routes.
Run-specific protocol digests remain distinct.

## Admission and terminal checks

Eight budgeted sessions rejoin their exact owner-derived logical allowances.
The four legacy sessions supply comparisons without aggregate resource admission.
Four actual Journal captures contain 112 verified exchange pairs.
The owner-derived normal budget for all twelve routes is 336 pairs.
Uncaptured routes supply no transcript authentication.

The terminal audit verifies 137 original artifact identities and 1,119,933 retained case bytes.
It rejoins typed exports, captured requests, ACKs, releases, Finish, command exits, and separate retirement receipts.
The campaign used no retry or replacement case.

The tool source gate passed 31 test-method executions with zero skips.
Seven body controls also run within the eleven captured controls.
Thirteen methods check selection and audit behavior.
Installed failures precede their dependent recorder or process effects, with paired accepted controls.
Constructor sentinels are synthetic.
Their controls do not execute the simulator.

The host remained shared, without timing or memory-performance claims.
The NCP full-gate terminal already existed at this campaign's start and remained present at completion.
This record therefore claims no concurrent NCP execution interval.

## Remaining limits

Logical allowances, sampled free space, capture capacity, and physical memory describe different facts.
These cases establish no aggregate operating-system, GPU, or process-memory ceiling.
They establish no real-time rate, physical calibration, broad stability, or full 7,200-tick recorder lifecycle.
This resource campaign did not assess continuous contact.
Overall tracking qualification remains false.
Historical sensor failures and earlier performance outcomes remain unchanged.
Complete v1 release qualification remains false.
